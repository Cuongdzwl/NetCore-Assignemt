@using NetCore_Assignemt.Areas.Identity.Data
@inject Microsoft.AspNetCore.Identity.UserManager<User> UserManager
@{
    ViewData["Title"] = "Home Page";
}

<!-- Example link to navigate to the role creation page -->
<a asp-controller="Admin" asp-action="SetRoles" asp-route-userId="@UserManager.GetUserId(User)">Set Roles</a>

@* <div class="List-book">
    <button class="btn btn-success" id="checkoutButton" onclick="checkOut()">Check Out</button>
    @foreach (var item in ViewBag.Book)
    {
        <div class="product-card">
            <a asp-action="Detail" asp-route-id="@item.BookId"><img src="/images/@item.Image" class="img-responsive" width="200" height="200" /></a>
            <span class="title"><a asp-action="Detail" asp-route-id="@item.BookId">@item.Title</a></span>
            <span class="title">@item.Publisher</span>
            <button onclick="addToCart(@item.BookId,1)">Add to Cart</button>
        </div>
    }
</div> *@
<div class="my-4 text-align:center">GreenWich BookStore</div>


<div class="row">
    <!-- Single Page Header start -->
    <div class="container-fluid page-header py-5">
        <h1 class="text-center text-white display-6">Shop</h1>
    </div>
    <!-- Book Shop Start-->
    <div class="container-fluid fruite py-5">
        <div class="container py-5">
            <h1 class="mb-4">Book Store</h1>
            <div class="row g-4">
                <div class="col-lg-12">
                    <div class="row g-4">
                        <div class="col-xl-3">
                            <div class="input-group w-100 mx-auto d-flex">
                                <input type="search" class="form-control p-3" placeholder="keywords" aria-describedby="search-icon-1">
                                <span id="search-icon-1" class="input-group-text p-3"><i class="fa fa-search"></i></span>
                            </div>
                        </div>
                        <div class="col-lg-9">
                            <div class="row g-4 justify-content-center">
                                @foreach (var item in ViewBag.Book)
                                {
                                    <div class="col-md-6 col-lg-6 col-xl-4">
                                        <div class="rounded position-relative">
                                            <div class="fruite-img">
                                                <a asp-controller="Home" asp-action="Details" asp-route-id="@item.BookId">
                                                    <img src="/images/@item.ImagePath" class="img-fluid w-100 rounded-top" alt="">
                                                </a>
                                            </div>
                                            <div class="text-white bg-secondary px-3 py-1 rounded position-absolute" style="top: 10px; left: 10px;">@item.BookCategories.Categories.Name</div>
                                            <div class="p-4 border border-secondary border-top-0 rounded-bottom">
                                                <h4>@item.Title</h4>
                                                <div class="d-flex justify-content-between flex-lg-wrap">
                                                    <p class="text-dark fs-5 fw-bold mb-0">$@item.Price</p>
                                                    <button onclick="addToCart(@item.BookId,1)" class="btn btn-primary float-right addToCartButton"><i name="" class="fa-solid fa-cart-shopping addToCartIcon"></i></button>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                }
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

